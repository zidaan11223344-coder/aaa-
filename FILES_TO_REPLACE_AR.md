# الملفات التي تستبدلها في مشروع البوت

> **هذه حزمة موحّدة مستقلة.** تحتوي لعبة مليون، إصلاح العربية في بطاقة مليون، وإصلاح أسماء الهدايا العربية؛ لا تحتاج إلى تنزيل أو تثبيت أي أرشيف تحديث سابق معها.

استبدل الملفات مع الحفاظ على **نفس المجلدات والأسماء**. لا تنسخ ملف `config.json` من الحزمة إذا كانت عندك بيانات اتصال وإعدادات خاصة؛ أبقِ ملفك الحالي كما هو.

| المسار داخل المشروع | هل يجب استبداله؟ | السبب |
|---|---:|---|
| `bot.py` | نعم | منطق بطاقات الألعاب والهدايا ولعبة مليون والدعوات والرسائل الخاصة الجماعية وتشغيل الموسيقى المحلي |
| `messages.json` | نعم | جميع قوالب الرسائل القابلة للتعديل، ومنها الدعوات والرسالة الجماعية ومهلة مليون |
| `assets/game_million_arabic_clear.jpg` | نعم | خلفية مليون الجديدة الخالية من النص المدمج؛ يكتب البوت العربية بوضوح فوقها |
| `assets/NotoSansArabic-SemiBold.ttf` | نعم | خط عربي واضح ومرفق لبطاقات النتائج؛ لا تحذفه |
| `games/testing/games.json` | نعم | تعريف لعبة مليون في وضع اختبار فقط |
| `games/testing/active_tests.json` | لا | اتركه كما هو؛ يبدأ فارغًا حتى تفعّل الاختبار |
| `README_التعديلات.md` | موصى به | دليل الأوامر الجديدة وتشغيل Spotify والرسائل الخاصة |
| `README_تعديل_الرسائل.md` | موصى به | دليل تعديل قوالب `messages.json` الجديدة |
| `requirements_pydroid.txt` | نعم عند استخدام Pydroid | يثبت Pillow و`arabic-reshaper` و`python-bidi` اللازمة لكتابة العربية داخل الصور |

> تصميم بطاقة النتيجة الموحد لكل الألعاب موجود داخل `bot.py` نفسه؛ لذلك لا تحتاج إلى استبدال صور الألعاب القديمة كي تظهر أسماء الفائزين واللاعبين بصورة منظمة وقابلة للنسخ في النص المرافق.

## أوامر الرسائل الخاصة والموسيقى

| الأمر | الصلاحية | النتيجة |
|---|---|---|
| `دعوة اسم_المستخدم` أو `دعوه اسم_المستخدم` | جميع المستخدمين | يرسل للهدف في الخاص: «اسم الداعي يدعوك لغرفة اسم الغرفة». |
| `جماعية@الرسالة` أو `جماعيه@الرسالة` | الماستر فقط | يرسل الرسالة في الخاص إلى أعضاء **الغرفة الحالية فقط**. |
| `تشغيل اسم_الأغنية` | بحسب إعداد توثيق VIP | تشغيل من YouTube داخل الغرفة الحالية فقط، بلا تفاعلات. |
| `.تشغيل اسم_الأغنية` أو `.تشغيل رابط_Spotify` | بحسب إعداد توثيق VIP | تشغيل/رابط Spotify داخل الغرفة الحالية فقط، بلا تفاعلات. |

## تشغيل لعبة مليون في وضع اختبار

بعد رفع الملفات وإعادة تشغيل البوت، أرسل للبوت في الخاص من حساب المالك:

```text
تشغيل اختبار@مليون
```

بعدها يستطيع أي مستخدم كتابة `مليون` داخل الغرفة. يرسل البوت أولًا `🔎 جاري البحث عن مليون...` ثم يختار النتيجة عشوائيًا. عند الفوز، ينشر بطاقة الفائز في كل الغرف. لكل مستخدم مهلة **دقيقتين**. لإيقافها بعد الاختبار، أرسل:

```text
إيقاف اختبار@مليون
```

لا تستخدم `اعتماد لعبة مليون` إلا بعد أن تتأكد من النتائج؛ قبل ذلك تبقى اللعبة خارج الألعاب الأساسية.
