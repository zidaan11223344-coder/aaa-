# مطور Giant Chat المحلي

- الذكاء الاصطناعي يعمل محلياً عبر llama.cpp/Qwen ولا يحتاج OPENAI_API_KEY.
- `اضف لعبة اسم | وصف` ينشئ تعريف اللعبة في `games/testing/games.json` فقط.
- الألعاب الموجودة في `testing` لا تُحمّل في نظام اللعب.
- `اعتماد لعبة command` ينقل التعريف إلى `custom_games.json` ويضع نسخة في `games/approved/`.
- أوامر التطوير والصيانة والاعتماد متاحة لصاحب البوت فقط في الخاص.
- `تشغيل التوثيق` / `إيقاف التوثيق` / `حالة التوثيق` تتحكم في حماية VIP.


## وضع اختبار الألعاب (المطور)
- `تشغيل اختبار@command` — يفعّل لعبة من `games/testing/games.json` للاختبار داخل الغرف، للمالك فقط.
- `إيقاف اختبار@command` — يوقف اختبار اللعبة.
- `حالة اختبار@command` — يعرض حالة الاختبار.
- `اختبارات نشطة` — يعرض الألعاب قيد الاختبار.
- `اعتماد لعبة command` — ينقل اللعبة من بيئة الاختبار إلى الألعاب المعتمدة.
- الألعاب الموجودة في `testing` لا تعمل تلقائيًا، ولا يستطيع المستخدم العادي تشغيل لعبة قيد الاختبار.
